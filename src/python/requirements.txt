# Python Dependencies for Bayesian Stellar Parameters
# 
# Install with: pip install -r requirements.txt

pandas>=1.3.0       # For data manipulation
numpy>=1.21.0       # For numerical computations
matplotlib>=3.4.0   # For plotting
seaborn>=0.11.0     # For statistical visualizations
scipy>=1.7.0        # For curve fitting and signal processing

pandas: For efficient handling and manipulation of tabular data (import pandas as pd).
numpy: For array manipulation and numerical operations (import numpy as np).
matplotlib: For creating plots and visualizations (import matplotlib.pyplot as plt).
Includes mpl_toolkits.mplot3d, which is part of matplotlib and does not require a separate library.
seaborn: For higher-level statistical visualizations (import seaborn as sns).
scipy: For advanced numerical and scientific functionality like curve_fit and medfilt.
